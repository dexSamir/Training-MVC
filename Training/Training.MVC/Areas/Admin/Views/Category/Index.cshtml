
@model List<Category>
@{
    ViewData["Title"] = "Category Page";
    int count = 1; 
}
<div class="container">
    <div class="row">
        <div class="col">
            <div class="card shadow mb-4">
                <div class="card-header py-3 d-flex justify-content-between align-items-center">
                    <h6 class="m-0 font-weight-bold text-primary">Category List</h6>
                    <a asp-action="Create" asp-controller="Category" class="btn btn-outline-primary">Create</a>
                </div>
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Name</th>
                                <th scope="col">Created Time</th>
                                <th scope="col">Update</th>
                                <th scope="col">Delete</th>
                                <th scope="col">Show/Hide</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td scope="col">@(count++)</td>
                                    <td scope="col">@item.Name</td>
                                    <td scope="col">@item.CreatedTime</td>
                                    <td scope="col">
                                        <a class="btn btn-outline-primary" asp-action="Update" asp-controller="Category" asp-route-id="@item.Id">
                                            <i class="fa-solid fa-pen"></i>
                                        </a>
                                    </td>
                                    <td scope="col">
                                        <a class="btn btn-outline-danger" asp-action="Delete" asp-controller="Category" asp-route-id="@item.Id">
                                            <i class="fa-solid fa-trash"></i>
                                        </a>
                                    </td>
                                    <td scope="col">
                                        <a class="btn @(item.IsDeleted ? "btn-outline-warning" : "btn-outline-secondary")"
                                           asp-action="@(item.IsDeleted ? "Show" : "Hide")"
                                           asp-controller="Category"
                                           asp-route-id="@item.Id">
                                            <i class="fa-solid @(item.IsDeleted ? "fa-eye-slash" : "fa-eye")"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>